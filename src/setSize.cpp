// Textfield - setSize.cpp

#ifndef _TEXTFIELD_
	#include "Textfield.hpp"
#endif

// setSize
// Sets the size of the outermost frame of
// the Textfield (most of the time the field
// showing already written content
void gg::Textfield::setSize(sf::Vector2u siz) {

	/*// Create temporary texture
	sf::Texture temp ;
	temp.create(siz.x , siz.y) ;

	m_writtenBox.setTexture(temp) ;*/

}